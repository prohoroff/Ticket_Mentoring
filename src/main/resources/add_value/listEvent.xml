<?xml version="1.0" encoding="UTF-8" ?>
<beans
        xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:util="http://www.springframework.org/schema/util"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/util
                        https://www.springframework.org/schema/util/spring-util.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="by.prohor.dao"/>
    <context:property-placeholder location="classpath:add_value/prop/event.properties"/>
    <!--Create Beans Event-->

    <bean id="event1" class="by.prohor.model.Event">
        <property name="id" value="${event.1.id}"/>
        <property name="title" value="${event.1.title}"/>
        <property name="date" value="${event.1.date}"/>
    </bean>

    <bean id="event2" class="by.prohor.model.Event">
        <property name="id" value="${event.2.id}"/>
        <property name="title" value="${event.2.title}"/>
        <property name="date" value="${event.2.date}"/>
    </bean>

    <bean id="event3" class="by.prohor.model.Event">
        <property name="id" value="${event.3.id}"/>
        <property name="title" value="${event.3.title}"/>
        <property name="date" value="${event.3.date}"/>
    </bean>

    <!--  Add value in ArrayList Event  -->
    <util:list id="events" list-class="java.util.ArrayList" value-type="by.prohor.model.Event">
        <ref bean="event1"/>
        <ref bean="event2"/>
        <ref bean="event3"/>
    </util:list>

</beans>
